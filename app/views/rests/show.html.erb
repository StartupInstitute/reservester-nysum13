<h1><center> Restaurant </center></h1>
<p id='notice'>
<%= notice %>
</p>

<table class="table table-bordered">
<tr>
	<td><%="Image"%></td>
	<td><%= image_tag @rest.image_url.to_s %></td>
</tr>
<tr>
	<td><%="Name"%></td>
	<td><%="Description"%></td>
	<td><%="Address"%></td>
	<td><%="Phone Number"%></td>
</tr>

<tr>
	<td><%= @rest.name %> | <%= link_to 'Edit', edit_rest_path(@rest) %> | <%= link_to 'Delete', rest_path(@rest), method: :delete %></td>
	<td><%= @rest.description %></td>
	<td><%= @rest.address %></td>
	<td><%= @rest.phone %></td>
</tr>
</table>
<p>
<%= link_to 'Back',rests_path %>
