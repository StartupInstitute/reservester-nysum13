<p id="notice"><%= notice %></p>

<h1>Reservation at <%= @restaurant.name %></h1>

<dl>
  <dt>Name</dt>
  <dd><%= @reservation.name %></dd> <!--Reservation.find(:id) <%= restaurant_reservation_path([@restaurant, @reservation]) %></h1>-->
  <dt>Date</dt>
  <dd><%= @reservation.date %></dd>
  <dt>Time</dt>
  <dd><%= @reservation.time.strftime("%I:%M%p") %></dd>
</dl>

<%= link_to 'Edit', edit_restaurant_reservation_path(@reservation) %> |
<%= link_to 'Delete', restaurant_reservation_path(@reservation), :method => :delete, :confirm => 'Are you sure that you want to delete this reservation?' %>
