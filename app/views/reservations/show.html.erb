<p id="notice"><%= notice %></p>

<h1>Reservation at <%= @restaurant.name %></h1>

<dl>
  <dt>Name</dt>
  <dd><%= @reservation.name %></dd> <!--Reservation.find(:id) <%= restaurant_reservation_path([@restaurant, @reservation]) %></h1>-->
  <dt>Date and Time</dt>
  <dd><%= @reservation.datetime %></dd>
</dl>

<%= link_to 'Edit', edit_restaurant_reservation_path([@restaurant, @reservation]) %> |
<%= link_to 'Destroy', restaurant_reservation_path([@restaurant, @reservation]), :method => :delete, :confirm => 'Are you sure that you want to delete this reservation?' %> |
<%= link_to 'Back', restaurants_path %>
