<%= form_for ([@reservation.rest, @reservation]) do |r| %>

  <%= r.label :username %>
  <%= r.text_field :username %><br>

  <%= r.label :reservation_time %>
  <%= r.datetime_select :reservation_time %><br>

  <%= r.submit %>
  <% end %>